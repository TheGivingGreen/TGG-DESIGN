Email capture, built to live on the Flag red band at the bottom of the marketing site.

```jsx
<EmailForm cta="Notify Me" onSubmit={handle} />
```

Only ever appears on Flag red. On sand, use `Input` plus a `Button`.
