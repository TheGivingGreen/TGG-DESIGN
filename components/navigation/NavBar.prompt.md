The site nav, in both states.

```jsx
<NavBar scrolled={y > 80} base="../.."
  links={[{label:'The Crawl',href:'#course'},{label:'Charities',href:'#good'},{label:'Pro Shop',href:'pro-shop.html'}]}
  cta="Get Tickets" ctaHref="https://thegivinggreen.store" />
```

- Over the hero: transparent, cream mark and wordmark, ghost CTA.
- Scrolled past 80px: sand background, 2px forest bottom rule, forest text, the one soft shadow in the system.
- The CTA always says get tickets. It is never a "Learn more".
