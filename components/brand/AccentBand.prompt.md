One-line Flag red statement band, used to close an argument.

```jsx
<AccentBand>Help Us Raise More. We'll Help You Sell More.</AccentBand>
```

If a page already has a Flag red CTA above the fold, use `tone="forest"` here instead. Flag red gets spent once.
